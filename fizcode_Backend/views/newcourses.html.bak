{{extend 'layout.html'}}
<h2>Nuevo Curso</h2>
    {{=form}}

<h1>Cursos</h1>
    <table class="table">
        {{=row in rows}}
        <tr>
            <td>{{=row.title}}</td>
            <td>{{=row.posted_on}}</td>
        </tr>
       {{pass}}
    </table>
